<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.appointment.dao.UserDao">
    <insert id="insertUser" parameterType="com.example.appointment.entity.UserBean">
        insert into tb_user(user_id, user_account, user_password, user_name, user_age, user_sex, user_position, checked)
        values(#{user.user_id}, #{user.user_account}, #{user.user_password}, #{user.user_name}, #{user.user_age},
        #{user.user_sex}, #{user.user_position}, 0)
    </insert>

    <select id="queryUser" resultType="com.example.appointment.entity.UserBean">
        select user_id, user_account, user_name, user_age, user_sex, user_position, checked from tb_user
        <where>
            <if test="user_account != null">
                user_account = #{user_account}
            </if>
            <if test="user_password != null">
                and user_password = #{user_password}
            </if>
            <if test="user_id != null">
                and user_id = #{user_id}
            </if>
            <if test="user_position != '-1'">
                and user_position = #{user_position}
            </if>
        </where>
    </select>

    <update id="updateById">
        update tb_user
        <set>
            <if test="columname != null">
                ${columname} = #{value}
            </if>
        </set>
        where user_id = #{user_id}
    </update>

    <delete id="deleteById">
        delete from tb_user where user_id = #{user_id}
    </delete>

    <delete id="deleleByIdList">
        delete from tb_user where user_id in
        <foreach collection="list" open="(" close=")" index="index" item="item" separator=",">
            #{item}
        </foreach>
    </delete>
</mapper>